# PyTorch with CUDA: install once via repo script (GPU wheels are not on default PyPI):
#   ..\..\scripts\setup_training_env_gpu.ps1
# Or manually:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

scipy
librosa
matplotlib
tqdm
seaborn
fasteners
PyYAML==6.0.2
